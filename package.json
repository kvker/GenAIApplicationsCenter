{
  "scripts": {
    "dev": "concurrently 'npm:anywhere' 'npm:tsc'",
    "tsc": "tsc -w",
    "anywhere": "anywhere -h localhost -d src",
    "build": "rm -rf dist && echo 'delete dist/ success, then do gulp... && gulp"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-htmlmin": "^5.0.1",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2",
    "typescript": "^5.2.2"
  }
}
