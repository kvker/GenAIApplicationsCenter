{
  "scripts": {
    "dev": "npm run anywhere",
    "anywhere": "anywhere -h localhost -d src",
    "gulp": "rm -rf dist && echo 'delete dist/ success, then do gulp... && gulp"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-clean-css": "^4.3.0",
    "gulp-htmlmin": "^5.0.1",
    "gulp-rename": "^2.0.0",
    "gulp-uglify": "^3.0.2"
  }
}